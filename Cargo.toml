[package]
name = "fieldx"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
fieldx_derive = { path = "fieldx_derive", version = "=0.1.0" }
parking_lot = "0.12"
num_cpus = "1.16"
crossbeam = "0.8"

[dev-dependencies]
trybuild = "1.0"
parking_lot = "0.12"
